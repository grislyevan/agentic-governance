# Agent Handoff: Forward Priorities Execution

**Date:** 2026-03-12
**Context:** Executing the plan in `.cursor/plans/forward_priorities_a_b_c_10407a05.plan.md`
**Task breakdown:** `docs/forward-priorities-tasklist.md` (42 tasks across A/B/C tiers)

---

## What's Done

### Priority A1: Demo Mode (INIT-37) - COMPLETE

All 10 tasks done. Committed as `be3653e`.

| File | What |
|------|------|
| `api/core/config.py` | Added `demo_mode: bool = False` setting |
| `api/core/demo_seed.py` | New. Seeds 3 endpoints (macOS/Win/Linux), ~50 events across 5 tools (Claude Code, Cursor, Ollama, Aider, OpenClaw). Covers detect/warn/block/escalate decisions, enforcement actions, posture changes, attribution updates, heartbeats. 7-day window, deterministic RNG (seed=42). |
| `api/routers/demo.py` | New. `GET /api/demo/status` and `POST /api/demo/reset` (owner-only, guarded by demo_mode flag) |
| `api/main.py` | Demo seed wired into `_seed()` when `DEMO_MODE=true`. Demo router registered. |
| `dashboard/src/components/layout/DemoBanner.jsx` | New. Amber banner, dismissible per session. |
| `dashboard/src/App.jsx` | Renders DemoBanner above TopBar. |
| `dashboard/src/lib/api.js` | Added `fetchDemoStatus()` export. |
| `api/tests/test_demo.py` | New. 10 tests covering status, reset, auth guards, idempotency, data integration. |
| `api/.env.example` | Documented `DEMO_MODE`. |

### Priority A2: Capability Brief (INIT-33) - COMPLETE

Committed as `8270fa7`.

| File | What |
|------|------|
| `branding/capability-brief.md` | One-page brief following INIT-33 structure. All claims backed by lab run IDs and test counts. |
| `branding/build-brief.sh` | Pandoc pipeline. Auto-detects PDF engine (pdflatex/xelatex/tectonic), falls back to HTML. |
| `.gitignore` | Added `branding/capability-brief.html` and `.pdf` as build artifacts. |

### Priority A3: Live Deploy - COMPLETE

| Commit | What |
|--------|------|
| `8c01438` | Version aligned to 0.1.0 (`pyproject.toml`, `collector/_version.py`). Dashboard pre-build step documented in `SERVER.md`. |
| `e17c92c` | Fixed release workflow: moved `secrets.*` out of step-level `if:` to job-level `env` block. |
| `b5a40e7` | Fixed dashboard artifact download path in release workflow. |
| `9c84d96` | Created `deploy/fly-deploy.sh` and `docs/session-first-cloud-deploy.md`. |
| `e294040` | Deployed to Windows VM (192.168.64.4) via bare-metal Python. E2E agent test passed. v0.1.0 tagged and pushed. |

### Priority B1: Stripe Billing - COMPLETE

Committed as `e294040`. 14 files changed, 1,168 insertions. 23 new tests.

### Priority B2: Secrets Management - COMPLETE

`docs/secrets-management.md` with decision (platform-native env vars), secrets inventory, rotation guidance. `SERVER.md` updated with per-platform setup.

### Priority B3: Uptime Monitoring - COMPLETE

`SERVER.md` updated with UptimeRobot setup, Grafana Cloud option, alert escalation table.

### Priority C1: SSO/OIDC - COMPLETE

9 tests. Full OIDC flow: authlib config, SSO login redirect, callback with user auto-provisioning, local login guard, dashboard SSO button, callback page.

### Priority C2: SIEM Connectors - COMPLETE

6 tests. 5 templates (Splunk HEC, Elastic, Sentinel, PagerDuty, Slack), `POST /webhooks/from-template`, dashboard template modal, `docs/siem-integration.md`.

### Priority C3: MITRE ATT&CK Mapping - COMPLETE

21 tests. Mapping module for BEH-001..008 and tool classes A-D, schema extension, event enrichment, dashboard ATT&CK column with badges/filters.

### Priority C4: Compliance Reporting - COMPLETE

12 tests. JSON/CSV/PDF export via reportlab, compliance summary endpoint, dashboard export modal with date range picker.

---

## Test Results

| Suite | Tests | Status |
|-------|-------|--------|
| API (`api/tests/`) | 244 | All passing |
| Collector (targeted: attack_mapping, behavioral, confidence) | 78 | All passing |
| Protocol (`protocol/tests/`) | 48 | All passing |
| Dashboard build | 72 modules | Clean |

---

## Git State

```
f6f446f feat: add enterprise features B2-C4 (SSO, SIEM, ATT&CK, compliance, secrets, monitoring)
f3e4846 docs: update all documentation for v0.1.0 release (A3 complete, B1 complete)
e294040 feat: add Stripe billing infrastructure with tier limits and dashboard (B1)
ba6d1f0 docs: add agent handoff for forward priorities execution
b5a40e7 fix(release): download dashboard artifact to correct path
9c84d96 chore: add Fly.io deploy script and first cloud deploy session doc
8c01438 chore: align version to 0.1.0 for first public release, document Fly pre-build step
8270fa7 docs: add one-page capability brief with evidence-backed claims (INIT-33)
be3653e feat: add demo mode with sample data seeding, reset endpoint, and dashboard banner (INIT-37)
```

Branch: `main`, pushed to origin at `f6f446f`. Tag `v0.1.0` on remote.

---

## What's Next

All forward priorities (A1-A3, B1-B3, C1-C4) are complete. The remaining work falls into five categories, ordered by impact.

### 1. Release Pipeline Fix (blocks future releases)

**Dashboard Docker image build failure.** The standalone `detec-dashboard` Docker build in `.github/workflows/release.yml` fails due to a Tailwind config issue in `dashboard/Dockerfile`. This blocks the GitHub Release creation step (which depends on all Docker jobs succeeding). The `detec-api` Docker image builds fine.

| Task | Effort | Files |
|------|--------|-------|
| Fix `dashboard/Dockerfile` Tailwind config | 1-2 hrs | `dashboard/Dockerfile`, possibly `dashboard/tailwind.config.js` or `dashboard/postcss.config.js` |
| Tag and push `v0.2.0` with all B/C features | 30 min | After Dockerfile fix |

### 2. Deploy Updated Server to Windows VM

The VM at 192.168.64.4 is running `v0.1.0`. The new features (SSO, SIEM, ATT&CK, compliance, billing) should be deployed.

| Task | Effort | Details |
|------|--------|--------|
| Pull latest on VM, rebuild dashboard, restart server | 30 min | `git pull && cd dashboard && npm run build && cd .. && restart service` |
| Set production secrets via system env vars | 15 min | Follow `docs/secrets-management.md` Windows section |
| Configure UptimeRobot external monitor | 15 min | Follow `SERVER.md` monitoring section |
| Test SSO with an IdP (optional, needs OIDC provider) | 1-2 hrs | Okta dev account or Azure AD |
| Test Stripe checkout in test mode (optional) | 1 hr | Stripe test keys |

### 3. Live EDR and Platform Integration

CrowdStrike, Jamf, and Intune integrations are stubbed. ESF helper isn't packaged.

| Task | Effort | Priority | Files |
|------|--------|----------|-------|
| CrowdStrike EDR query methods (process, network, file) | 1-2 days | Medium | `api/integrations/crowdstrike.py` |
| Jamf enforcement provider | 2-3 days | Low | `api/integrations/jamf_enforcement.py` |
| Intune enforcement provider | 2-3 days | Low | `api/integrations/intune_enforcement.py` |
| ESF helper packaging into macOS .app | 1 day | Low | `packaging/macos/` |

All of these require vendor sandbox access or hardware, so they can be deferred until a customer needs them.

### 4. Testing and Performance Hardening

| Task | Effort | Priority | Details |
|------|--------|----------|---------|
| `main.py` integration tests (E2E with scanner stubs) | 1-2 days | Medium | `collector/tests/test_main_integration.py` |
| Behavioral scanner 500ms benchmark | 2-4 hrs | Low | Load 10k events, assert scan completes under 500ms |
| Native telemetry 100ms latency benchmark | 2-4 hrs | Low | Requires platform CI runners |
| Linux cgroup v2 net_cls integration tests | 1 day | Low | Requires privileged Linux CI runner |
| CI platform matrix (macOS, Windows, Linux runners) | 1-2 days | Low | For ESF/ETW/eBPF provider tests |

### 5. Product and Business

| Task | Effort | Priority | Details |
|------|--------|----------|---------|
| Create org flow UI (multi-tenant switching) | 2-3 days | Medium | API exists (`PROGRESS.md` M3 item); no dashboard UI |
| Evasion detection suite (INIT-29) | 3-5 days | Medium | Builds on LAB-RUN-EVASION-001 |
| Live lab validation for 5 new scanners | 2-3 days | Low | Aider, GPT-Pilot, Cline, LM Studio, Continue; synthetic done, live pending |
| Playbook v0.5 with new lab run findings | 2-3 days | Low | 20+ lab runs listed in playbook "Next Actions" |
| Multi-region deployment option | 3-5 days | Low | Not started |
| SLA and support tier documentation | 1 day | Low | Not started |
| Triage INIT-28 through INIT-42 | 2-4 hrs | Low | Shelved issues needing review |
| Deprecate `--enforce` CLI flag | 1 hr | Low | Warn on use; server posture replaces it |

### 6. Miscellaneous Cleanup

| Task | Effort | Details |
|------|--------|---------|
| Update `PROGRESS.md` checkboxes for B2, B3, C1-C4 | Done this session | M4 and M5 items now checked |
| Update `docs/forward-priorities-tasklist.md` status markers | 30 min | Change `[ ]` to `[x]` for all completed tasks |
| Update `AGENTS.md` test counts | 15 min | API tests now 244 (was 216), add C1-C4 context |

---

## Recommended Priority Order

If picking up tomorrow:

1. **Fix the dashboard Dockerfile** (unblocks release pipeline).
2. **Deploy to VM** (put the new features in front of users).
3. **Tag v0.2.0** (captures SSO, SIEM, ATT&CK, compliance, billing).
4. **Create org flow UI** (the last M3 checkbox).
5. **Evasion detection suite** (the last high-value M5 item that doesn't need vendor access).

---

## Key Files for Context

| File | Purpose |
|------|---------|
| `PROGRESS.md` | Milestone tracker (M1-M5 checkboxes) |
| `docs/forward-priorities-tasklist.md` | 42-task breakdown, all complete |
| `AGENTS.md` | Project architecture and conventions |
| `api/core/config.py` | All settings (OIDC, Stripe, EDR, SMTP, enforcement) |
| `docs/secrets-management.md` | Secrets approach decision and inventory |
| `docs/siem-integration.md` | SIEM connector setup guides |
| `docs/enforcement-roadmap.md` | Enforcement phases 1-6, all complete, remaining items noted |
| `schemas/canonical-event-schema.json` | v0.4.0 schema (now with MITRE ATT&CK) |
