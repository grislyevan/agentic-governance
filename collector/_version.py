"""Single source of truth for Detec Agent version and build number.

Imported by the GUI status window, PyInstaller spec, and anywhere else
that needs the agent version at runtime or build time.
"""

__version__ = "0.1"
__build__ = "0.1.0"
